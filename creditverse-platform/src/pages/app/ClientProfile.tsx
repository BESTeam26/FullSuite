/**
 * The canonical client record — who this person is, and what the organization
 * does for them. The credit-repair application lives in CreditOps.
 */
import { ClientProfilePage } from "@/components/clients/profile/ClientProfilePage";

const ClientProfile = () => <ClientProfilePage />;

export default ClientProfile;
